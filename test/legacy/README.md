# Legacy Test Helpers

This folder holds preserved historical implementations that are no longer part
of the core production path, but are still useful as reference code for
one-off studies and benchmark comparisons.

Current contents:

- `SPECTRALSOURCETERM_INLINE.m`
  - historical fully inlined spectral source-term implementation
  - kept only so the spectral perf study can compare the current organized
    functions path against the former inline organization
